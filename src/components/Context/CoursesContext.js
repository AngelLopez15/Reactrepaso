import React from 'react';

// el contex recibe como parametro el estado inicial

// Esta es una opcion, pero en este ejemplo se va a ocupar la sobre escritura del context
// const CoursesContext = React.createContext({
//   courses: []
// })

const CoursesContext = React.createContext()

export default CoursesContext